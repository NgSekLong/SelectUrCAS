cas.authn.mongo:
  host: ${AUTHENTICATION_MONGODB}
  userId: root
  password: ThisIsThePasswordForRoot
  databaseName: ${AUTHENTICATION_MONGODB}
  authenticationDatabaseName: admin
  #MongoDB won't work if there are no attributes as of CAS 6.1.1 ... probably is a bug
  #Since don't want to add additional attributes, I am using `_id` as the attribute :D
  attributes: _id