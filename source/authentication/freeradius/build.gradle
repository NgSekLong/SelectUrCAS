implementation "org.apereo.cas:cas-server-support-radius:${casServerVersion}"
// The below is needed due to a bug in CAS
implementation "org.apereo.cas:cas-server-support-simple-mfa:${casServerVersion}"

